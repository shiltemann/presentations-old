# GitPitch with GitLab

For offline development, run gitlab locally to test gitpitch


```
docker compose up
```

add remote for local gitlab

```
$ git remote add gitlab http://gitlab/shiltemann/presentations.git
$ git push gitlab
```
